# DeepFace-Detection-Model
This model is a simply Jupyter Notebook that is useful for Deep Face Detection i.e. detection of a person's face in a video feed by creating a bounding box around the face.
